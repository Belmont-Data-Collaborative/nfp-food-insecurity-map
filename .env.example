# AWS S3 Configuration (required for production mode)
BDAIC_BUCKET=your-s3-bucket-name

# Local Development Mode
# Set to "true" to use mock data files instead of S3
USE_MOCK_DATA=true

# Directory containing mock data files (relative to project root)
# Only used when USE_MOCK_DATA=true
MOCK_DATA_DIR=data/mock
